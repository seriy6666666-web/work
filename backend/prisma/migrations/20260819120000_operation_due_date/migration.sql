-- Срок самой операции: к какому дню участок должен её сдать.
--
-- До сих пор срок был один — дата отгрузки заказа, и она показывалась на всех
-- операциях подряд. Начальнику участка это ничего не давало: заготовки видели
-- дату, к которой изделие уходит заказчику, хотя их часть нужна сборке гораздо
-- раньше. Отличить «нужно послезавтра» от «нужно через три недели» было нельзя.
--
-- Необязательное: где срок не задан, показываем срок заказа, как было.
ALTER TABLE "Operation" ADD COLUMN "dueDate" TIMESTAMP(3);
