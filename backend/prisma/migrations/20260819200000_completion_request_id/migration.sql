-- Ключ последней отправки отметки о выработке.
--
-- Связь в цеху рвётся, планшет отправляет отметку повторно. Количество от этого
-- не портится — оно записывается целиком, а не прибавляется. Но повтор считался
-- бы исправлением, а их у рабочего всего два: система отказала бы человеку,
-- который ничего не исправлял, из-за оборвавшегося вайфая.
ALTER TABLE "CompletionRecord" ADD COLUMN "lastRequestId" TEXT;
