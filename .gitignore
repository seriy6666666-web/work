node_modules/
dist/
build/
generated/
.env
.env.*.local
*.log
.DS_Store
.claude/settings.local.json
test-results/
playwright-report/
backups/

# Дампы базы переносим руками, в репозиторий они не попадают
*-dump.sql
dump.sql

# Корневой сертификат Caddy — свой на каждой установке, выгружается командой из README
certs/

# Office owner-lock temp files
~$*
